import { Component } from "react";

class TiposUsuarios extends Component{
    constructor(props)
    {
        super(props);
        this.state = {
            // Abaixo, a estrutura do state (estado)
            // nomeEstado : valorInicial
        }
    }
}